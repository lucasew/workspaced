{{/* Access module config via .module */}}
{{- $mod := .module -}}

{{ $mod.greeting }}

This is an example file generated by workspaced.

{{- if $mod.show_hostname }}

Current hostname: (check OS environment)
{{- end }}

{{- if .root.hosts }}

Known hosts in config:
{{- range $name, $host := .root.hosts }}
  - {{ $name }}{{ if $host.ips }}: {{ index $host.ips 0 }}{{ end }}
{{- end }}
{{- end }}

{{- if .root.desktop.dark_mode }}

Dark mode: enabled
{{- end }}

---
This file demonstrates workspaced templates:

Module config access:
  {{ "{{ $mod := .module }}" }}
  {{ "{{ $mod.greeting }}" }}

Global config access:
  {{ "{{ .root.desktop.dark_mode }}" }}
  {{ "{{ .root.browser.default }}" }}

Loops over maps:
  {{ "{{ range $name, $host := .root.hosts }}" }}
  {{ "  {{ $name }}: {{ index $host.ips 0 }}" }}
  {{ "{{ end }}" }}

Conditionals:
  {{ "{{ if .root.desktop.dark_mode }}...{{ end }}" }}
