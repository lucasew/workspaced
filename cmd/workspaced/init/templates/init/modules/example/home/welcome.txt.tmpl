{{/* Access module config via .Modules map */}}
{{- $mod := index .Modules "example" -}}

{{ $mod.greeting }}

This is an example file generated by workspaced.

{{- if $mod.show_hostname }}

Current hostname: (check OS environment)
{{- end }}

{{- if .Hosts }}

Known hosts in config:
{{- range $name, $host := .Hosts }}
  - {{ $name }}{{ if $host.IPs }}: {{ index $host.IPs 0 }}{{ end }}
{{- end }}
{{- end }}

{{- if .Desktop.DarkMode }}

Dark mode: enabled
{{- end }}

---
This file demonstrates workspaced templates:

Module config access:
  {{ "{{ $mod := index .Modules \"example\" }}" }}
  {{ "{{ $mod.greeting }}" }}

Global config access:
  {{ "{{ .Desktop.DarkMode }}" }}
  {{ "{{ .Browser.Default }}" }}

Loops over maps:
  {{ "{{ range $name, $host := .Hosts }}" }}
  {{ "  {{ $name }}: {{ index $host.IPs 0 }}" }}
  {{ "{{ end }}" }}

Conditionals:
  {{ "{{ if .Desktop.DarkMode }}...{{ end }}" }}
